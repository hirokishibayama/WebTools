# content/ (legacy)

Prefer:

- `jobs/` — factory jobs
- `articles/` — published articles
- `assets/` — per-slug artifacts

`content/drafts/` is deprecated and kept only for compatibility with the earlier editorial pipeline.
